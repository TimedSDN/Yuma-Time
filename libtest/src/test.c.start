
/* 

*** Generated by yangdump 0.9.8.519M
*** Copyright (c) 2009, Netconf Central, Inc., All Rights Reserved.

    module test
    revision 2009-11-20

    namespace http://netconfcentral.com/ns/test
    organization Netconf Central, Inc.

 */

#include <xmlstring.h>

#ifndef _H_procdefs
#include "procdefs.h"
#endif

#ifndef _H_agt
#include "agt.h"
#endif

#ifndef _H_agt_cb
#include "agt_cb.h"
#endif

#ifndef _H_agt_not
#include "agt_not.h"
#endif

#ifndef _H_agt_rpc
#include "agt_rpc.h"
#endif

#ifndef _H_agt_util
#include "agt_util.h"
#endif

#ifndef _H_dlq
#include "dlq.h"
#endif

#ifndef _H_ncx
#include "ncx.h"
#endif

#ifndef _H_ncxmod
#include "ncxmod.h"
#endif

#ifndef _H_ncxtypes
#include "ncxtypes.h"
#endif

#ifndef _H_rpc
#include "rpc.h"
#endif

#ifndef _H_ses
#include "ses.h"
#endif

#ifndef _H_status
#include "status.h"
#endif

#ifndef _H_val
#include "val.h"
#endif

#ifndef _H_val_util
#include "val_util.h"
#endif

#ifndef _H_xml_util
#include "xml_util.h"
#endif

#ifndef _H_test
#include "test.h"
#endif

/* module static variables */
static ncx_module_t *test_mod;
static obj_template_t *test_obj;
static obj_template_t *make_test_obj;
static obj_template_t *cancel_test_obj;
static obj_template_t *testDone_obj;
static val_value_t *test_val;

/* put your static variables here */


/********************************************************************
* FUNCTION y_test_test_testManufacturer_get
* 
* Get database object callback
* Path: /test/testManufacturer
* Fill in 'dstval' contents
* 
* INPUTS:
*     see ncx/getcb.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_test_testManufacturer_get (
        ses_cb_t *scb,
        getcb_mode_t cbmode,
        const val_value_t *virval,
        val_value_t *dstval)
{
    status_t res;
    const xmlChar *testManufacturer;

    res = NO_ERR;

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if virval is used */
    (void)virval;

    if (cbmode != GETCB_GET_VALUE) {
        return ERR_NCX_OPERATION_NOT_SUPPORTED;
    }

    /* set the testManufacturer var here, change EMPTY_STRING */
    testManufacturer = EMPTY_STRING;
    res = val_set_simval_obj(
        dstval,
        dstval->obj,
        testManufacturer);

    return res;

} /* y_test_test_testManufacturer_get */


/********************************************************************
* FUNCTION y_test_test_testModelNumber_get
* 
* Get database object callback
* Path: /test/testModelNumber
* Fill in 'dstval' contents
* 
* INPUTS:
*     see ncx/getcb.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_test_testModelNumber_get (
        ses_cb_t *scb,
        getcb_mode_t cbmode,
        const val_value_t *virval,
        val_value_t *dstval)
{
    status_t res;
    const xmlChar *testModelNumber;

    res = NO_ERR;

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if virval is used */
    (void)virval;

    if (cbmode != GETCB_GET_VALUE) {
        return ERR_NCX_OPERATION_NOT_SUPPORTED;
    }

    /* set the testModelNumber var here, change EMPTY_STRING */
    testModelNumber = EMPTY_STRING;
    res = val_set_simval_obj(
        dstval,
        dstval->obj,
        testModelNumber);

    return res;

} /* y_test_test_testModelNumber_get */


/********************************************************************
* FUNCTION y_test_test_testStatus_get
* 
* Get database object callback
* Path: /test/testStatus
* Fill in 'dstval' contents
* 
* INPUTS:
*     see ncx/getcb.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_test_testStatus_get (
        ses_cb_t *scb,
        getcb_mode_t cbmode,
        const val_value_t *virval,
        val_value_t *dstval)
{
    status_t res;
    const xmlChar *testStatus;

    res = NO_ERR;

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if virval is used */
    (void)virval;

    if (cbmode != GETCB_GET_VALUE) {
        return ERR_NCX_OPERATION_NOT_SUPPORTED;
    }

    /* set the testStatus var here, change enum */
    testStatus = (const xmlChar *)"up";
    res = val_set_simval_obj(
        dstval,
        dstval->obj,
        testStatus);

    return res;

} /* y_test_test_testStatus_get */


/********************************************************************
* FUNCTION y_test_test_mro
* 
* Make read-only child nodes
* Path: /test
* 
* INPUTS:
*     parentval == the parent struct to use for new child nodes
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_test_mro (val_value_t *parentval)
{
    status_t res;
    val_value_t *childval;

    res = NO_ERR;

    /* add /test/testManufacturer */
    childval = agt_make_virtual_leaf(
        parentval->obj,
        y_test_N_testManufacturer,
        y_test_test_testManufacturer_get,
        &res);
    if (childval != NULL) {
        val_add_child(childval, parentval);
    } else {
        return res;
    }

    /* add /test/testModelNumber */
    childval = agt_make_virtual_leaf(
        parentval->obj,
        y_test_N_testModelNumber,
        y_test_test_testModelNumber_get,
        &res);
    if (childval != NULL) {
        val_add_child(childval, parentval);
    } else {
        return res;
    }

    /* add /test/testStatus */
    childval = agt_make_virtual_leaf(
        parentval->obj,
        y_test_N_testStatus,
        y_test_test_testStatus_get,
        &res);
    if (childval != NULL) {
        val_add_child(childval, parentval);
    } else {
        return res;
    }

    return res;

} /* y_test_test_mro */


/********************************************************************
* FUNCTION y_test_test_edit
* 
* Edit database object callback
* Path: /test
* Add object instrumentation in COMMIT phase.
* 
* INPUTS:
*     see agt/agt_cb.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_test_edit (
        ses_cb_t *scb,
        rpc_msg_t *msg,
        agt_cbtyp_t cbtyp,
        op_editop_t editop,
        val_value_t *newval,
        val_value_t *curval)
{
    status_t res;
    val_value_t *errorval;
    const xmlChar *errorstr;

    res = NO_ERR;
    errorval = NULL;
    errorstr = NULL;

    switch (cbtyp) {
    case AGT_CB_VALIDATE:
        /* description-stmt validation here */
        break;
    case AGT_CB_APPLY:
        /* database manipulation done here */
        break;
    case AGT_CB_COMMIT:
        /* device instrumentation done here */
        switch (editop) {
        case OP_EDITOP_LOAD:
            break;
        case OP_EDITOP_MERGE:
            break;
        case OP_EDITOP_REPLACE:
            break;
        case OP_EDITOP_CREATE:
            break;
        case OP_EDITOP_DELETE:
            break;
        default:
            res = SET_ERROR(ERR_INTERNAL_VAL);
        }

        if (res == NO_ERR) {
            res = agt_check_cache(
                &test_val,
                newval,
                curval,
                editop);
        }
        
        if (res == NO_ERR &&
            (editop == OP_EDITOP_LOAD || editop == OP_EDITOP_CREATE)) {
            res = y_test_test_mro(newval);
        }
        break;
    case AGT_CB_ROLLBACK:
        /* undo device instrumentation here */
        break;
    default:
        res = SET_ERROR(ERR_INTERNAL_VAL);
    }

    /* if error: set the res, errorstr, and errorval parms */
    if (res != NO_ERR) {
        agt_record_error(
            scb,
            &msg->mhdr,
            NCX_LAYER_CONTENT,
            res,
            NULL,
            NCX_NT_STRING,
            errorstr,
            NCX_NT_VAL,
            errorval);
    }
    
    return res;

} /* y_test_test_edit */


/********************************************************************
* FUNCTION y_test_make_test_validate
* 
* RPC validation phase
* All YANG constriants have passed at this point.
* Add description-stmt checks in this function.
* 
* INPUTS:
*     see agt/agt_rpc.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_make_test_validate (
        ses_cb_t *scb,
        rpc_msg_t *msg,
        xml_node_t *methnode)
{
    status_t res;
    val_value_t *errorval;
    const xmlChar *errorstr;
    val_value_t *testDoneness_val;
    val_value_t *testToastType_val;
    uint32 testDoneness;
    val_idref_t *testToastType;

    res = NO_ERR;
    errorval = NULL;
    errorstr = NULL;

    testDoneness_val = val_find_child(
        msg->rpc_input,
        y_test_M_test,
        y_test_N_testDoneness);
    if (testDoneness_val != NULL && testDoneness_val->res == NO_ERR) {
        testDoneness = VAL_UINT(testDoneness_val);
    }

    testToastType_val = val_find_child(
        msg->rpc_input,
        y_test_M_test,
        y_test_N_testToastType);
    if (testToastType_val != NULL && testToastType_val->res == NO_ERR) {
        testToastType = VAL_IDREF(testToastType_val);
    }

    /* if error: set the res, errorstr, and errorval parms */
    if (res != NO_ERR) {
        agt_record_error(
            scb,
            &msg->mhdr,
            NCX_LAYER_OPERATION,
            res,
            methnode,
            NCX_NT_STRING,
            errorstr,
            NCX_NT_VAL,
            errorval);
    }
    
    return res;

} /* y_test_make_test_validate */


/********************************************************************
* FUNCTION y_test_make_test_invoke
* 
* RPC invocation phase
* All constriants have passed at this point.
* Call device instrumentation code in this function.
* 
* INPUTS:
*     see agt/agt_rpc.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_make_test_invoke (
        ses_cb_t *scb,
        rpc_msg_t *msg,
        xml_node_t *methnode)
{
    status_t res;
    val_value_t *testDoneness_val;
    val_value_t *testToastType_val;
    uint32 testDoneness;
    val_idref_t *testToastType;

    res = NO_ERR;

    testDoneness_val = val_find_child(
        msg->rpc_input,
        y_test_M_test,
        y_test_N_testDoneness);
    if (testDoneness_val != NULL && testDoneness_val->res == NO_ERR) {
        testDoneness = VAL_UINT(testDoneness_val);
    }

    testToastType_val = val_find_child(
        msg->rpc_input,
        y_test_M_test,
        y_test_N_testToastType);
    if (testToastType_val != NULL && testToastType_val->res == NO_ERR) {
        testToastType = VAL_IDREF(testToastType_val);
    }

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if methnode is used */
    (void)methnode;

    /* invoke your device instrumentation code here */
    
    return res;

} /* y_test_make_test_invoke */


/********************************************************************
* FUNCTION y_test_cancel_test_validate
* 
* RPC validation phase
* All YANG constriants have passed at this point.
* Add description-stmt checks in this function.
* 
* INPUTS:
*     see agt/agt_rpc.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_cancel_test_validate (
        ses_cb_t *scb,
        rpc_msg_t *msg,
        xml_node_t *methnode)
{
    status_t res;
    val_value_t *errorval;
    const xmlChar *errorstr;

    res = NO_ERR;
    errorval = NULL;
    errorstr = NULL;

    /* if error: set the res, errorstr, and errorval parms */
    if (res != NO_ERR) {
        agt_record_error(
            scb,
            &msg->mhdr,
            NCX_LAYER_OPERATION,
            res,
            methnode,
            NCX_NT_STRING,
            errorstr,
            NCX_NT_VAL,
            errorval);
    }
    
    return res;

} /* y_test_cancel_test_validate */


/********************************************************************
* FUNCTION y_test_cancel_test_invoke
* 
* RPC invocation phase
* All constriants have passed at this point.
* Call device instrumentation code in this function.
* 
* INPUTS:
*     see agt/agt_rpc.h for details
* 
* RETURNS:
*     error status
********************************************************************/
static status_t
    y_test_cancel_test_invoke (
        ses_cb_t *scb,
        rpc_msg_t *msg,
        xml_node_t *methnode)
{
    status_t res;

    res = NO_ERR;

    /* remove the next line if scb is used */
    (void)scb;

    /* remove the next line if msg is used */
    (void)msg;

    /* remove the next line if methnode is used */
    (void)methnode;

    /* invoke your device instrumentation code here */
    
    return res;

} /* y_test_cancel_test_invoke */


/********************************************************************
* FUNCTION y_test_init_static_vars
* 
* initialize module static variables
* 
********************************************************************/
static void
    y_test_init_static_vars (void)
{
    test_mod = NULL;
    test_obj = NULL;
    make_test_obj = NULL;
    cancel_test_obj = NULL;
    testDone_obj = NULL;
    test_val = NULL;

    /* init your static variables here */

} /* y_test_init_static_vars */


/********************************************************************
* FUNCTION y_test_testDone_send
* 
* Send a y_test_testDone notification
* Called by your code when notification event occurs
* 
********************************************************************/
void
    y_test_testDone_send (
        const xmlChar *testStatus)
{
    agt_not_msg_t *notif;
    val_value_t *parmval;
    status_t res;

    res = NO_ERR;

    if (LOGDEBUG) {
        log_debug("\nGenerating <testDone> notification");
    }
    
    notif = agt_not_new_notification(testDone_obj);
    if (notif == NULL) {
        log_error("\nError: malloc failed, cannot send <testDone> notification");
        return;
    }
    
    /* add testStatus to payload */
    parmval = agt_make_leaf(
        testDone_obj,
        y_test_N_testStatus,
        testStatus,
        &res);
    if (parmval == NULL) {
        log_error(
            "\nError: make leaf failed (%s), cannot send <testDone> notification",
            get_error_string(res));
    } else {
        agt_not_add_to_payload(notif, parmval);
    }
    
    agt_not_queue_notification(notif);
    
} /* y_test_testDone_send */


/********************************************************************
* FUNCTION y_test_init
* 
* initialize the test server instrumentation library
* 
* INPUTS:
*    modname == requested module name
*    revision == requested version (NULL for any)
* 
* RETURNS:
*     error status
********************************************************************/
status_t
    y_test_init (
        const xmlChar *modname,
        const xmlChar *revision)
{
    agt_profile_t *agt_profile;
    status_t res;

    y_test_init_static_vars();

    /* change if custom handling done */
    if (xml_strcmp(modname, y_test_M_test)) {
        return ERR_NCX_UNKNOWN_MODULE;
    }

    if (revision && xml_strcmp(revision, y_test_R_test)) {
        return ERR_NCX_WRONG_VERSION;
    }

    agt_profile = agt_get_profile();

    res = ncxmod_load_module(
        y_test_M_test,
        y_test_R_test,
        &agt_profile->agt_savedevQ,
        &test_mod);
    if (res != NO_ERR) {
        return res;
    }
    
    test_obj = ncx_find_object(
        test_mod,
        y_test_N_test);
    if (test_mod == NULL) {
        return SET_ERROR(ERR_NCX_DEF_NOT_FOUND);
    }
    
    make_test_obj = ncx_find_object(
        test_mod,
        y_test_N_make_test);
    if (test_mod == NULL) {
        return SET_ERROR(ERR_NCX_DEF_NOT_FOUND);
    }
    
    cancel_test_obj = ncx_find_object(
        test_mod,
        y_test_N_cancel_test);
    if (test_mod == NULL) {
        return SET_ERROR(ERR_NCX_DEF_NOT_FOUND);
    }
    
    testDone_obj = ncx_find_object(
        test_mod,
        y_test_N_testDone);
    if (test_mod == NULL) {
        return SET_ERROR(ERR_NCX_DEF_NOT_FOUND);
    }
    
    res = agt_rpc_register_method(
        y_test_M_test,
        y_test_N_make_test,
        AGT_RPC_PH_VALIDATE,
        y_test_make_test_validate);
    if (res != NO_ERR) {
        return res;
    }
    
    res = agt_rpc_register_method(
        y_test_M_test,
        y_test_N_make_test,
        AGT_RPC_PH_INVOKE,
        y_test_make_test_invoke);
    if (res != NO_ERR) {
        return res;
    }
    
    res = agt_rpc_register_method(
        y_test_M_test,
        y_test_N_cancel_test,
        AGT_RPC_PH_VALIDATE,
        y_test_cancel_test_validate);
    if (res != NO_ERR) {
        return res;
    }
    
    res = agt_rpc_register_method(
        y_test_M_test,
        y_test_N_cancel_test,
        AGT_RPC_PH_INVOKE,
        y_test_cancel_test_invoke);
    if (res != NO_ERR) {
        return res;
    }
    
    res = agt_cb_register_callback(
        y_test_M_test,
        (const xmlChar *)"/test",
        (const xmlChar *)"2009-11-20",
        y_test_test_edit);
    if (res != NO_ERR) {
        return res;
    }
    
    /* put your module initialization code here */
    
    return res;
} /* y_test_init */


/********************************************************************
* FUNCTION y_test_init2
* 
* SIL init phase 2: non-config data structures
* Called after running config is loaded
* 
* RETURNS:
*     error status
********************************************************************/
status_t
    y_test_init2 (void)
{
    status_t res;

    res = NO_ERR;

    test_val = agt_init_cache(
        y_test_M_test,
        y_test_N_test,
        &res);
    if (res != NO_ERR) {
        return res;
    }
    
    /* put your init2 code here */
    
    return res;
} /* y_test_init2 */


/********************************************************************
* FUNCTION y_test_cleanup
*    cleanup the server instrumentation library
* 
********************************************************************/
void
    y_test_cleanup (void)
{
    agt_rpc_unregister_method(
        y_test_M_test,
        y_test_N_make_test);

    agt_rpc_unregister_method(
        y_test_M_test,
        y_test_N_cancel_test);

    agt_cb_unregister_callbacks(
        y_test_M_test,
        (const xmlChar *)"/test");

    /* put your cleanup code here */
    
} /* y_test_cleanup */

/* END test.c */
